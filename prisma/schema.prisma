// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model WorkLog {
  id        Int      @id @default(autoincrement())
  date      DateTime @default(now()) // 記録日時
  duration  Int      // 秒単位の作業時間
}